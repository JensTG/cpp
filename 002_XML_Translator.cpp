#include <iostream>
using namespace std;

int main(string args[]) {
   string inputFile = "0";
   string outputFile = "0";

   int argSize = 
   
   for (int i = 0; i < (sizeof(args) / sizeof(string)) - 1; i++) {
      switch (args[i]) {
         case "-o":
break;
}

}
